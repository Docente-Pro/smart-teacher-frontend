import { IProcesoBase } from './IProcesoBase';

/**
 * Interfaz para procesos del área de Personal Social
 * Incluye propiedades específicas para actividades de desarrollo personal, social y ciudadano
 */
export interface IProcesoPersonalSocial extends IProcesoBase {
  // Futuras propiedades específicas de Personal Social
  // Ejemplo:
  // actividadReflexiva?: string;
  // casoEstudio?: string;
  // rolePlay?: string;
}
