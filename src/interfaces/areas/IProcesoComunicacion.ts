import { IProcesoBase } from './IProcesoBase';

/**
 * Interfaz para procesos del área de Comunicación
 * Incluye propiedades específicas para actividades de lectura, escritura y comprensión
 */
export interface IProcesoComunicacion extends IProcesoBase {
  // Futuras propiedades específicas de Comunicación
  // Ejemplo:
  // textoLiterario?: string;
  // tipoTexto?: 'narrativo' | 'descriptivo' | 'argumentativo' | 'instructivo';
  // preguntasComprension?: IPreguntaComprension[];
}
